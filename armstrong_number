class Solution {
    static boolean armstrongNumber(int n) {
        int copy=n;
        int r=0;
        while(n>0){
            int d=n%10;
            r=r+(int)Math.pow(d,3);
            n=n/10;
        }
        return r==copy;
    }
}
